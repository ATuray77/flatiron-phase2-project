import React from 'react'
import { Link } from 'react-router-dom'



function SongsList({ songs }) {


  

// const renderSongs = Object.keys(songs).map((songID) => (
//   <li key={songID}>
//     <Link to={`/songs/${songID}`}>{songs[songID].}</Link>

//   </li>
//))

  return (
    <div>SongsList</div>
  )
}

export default SongsList